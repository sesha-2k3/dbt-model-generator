1. Complex source -> target mapping includes complex lookups, transformations, date validations, 
multi-step validations for a single column, etc.
2. sources.yaml and 
3. DBT tests (test.yaml)for table. Used to validate the data produced by the DBT models.
Examples:
order_id is unique
customer_id is never NULL
status only contains allowed values
Every order has a valid customer
Amounts are non-negative
Dates are within expected ranges
4. DBT package which includes models, sources, tests, seeds

01/26/2026
Next:
1. Provide option to give additional user prompts about the transformation logic/csv.
2. Code comments.



Additional:
1. Use cache to store the frequent SQL transformations and then make less LLM calls.
2. 